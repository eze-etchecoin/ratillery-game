# Agents

Documentation of the agent graph that drives development.

- `graph.md` — workflow diagram, agent/skill inventory, and design
  principles (artifact coordination, merge gate, escalation levels).

The executable definitions live in `.opencode/agents/` and
`.opencode/skills/`; this folder documents how they fit together.
