# Prompts

Human-authored task prompts that bootstrap larger pieces of work (e.g.
project initialization). Numbered sequentially: `001-...md`, `002-...md`.

These are inputs to the agent workflow, not agent outputs. Once executed,
their effects are tracked through `docs/status.md` and the corresponding
artifacts.
